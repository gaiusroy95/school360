import { CmsModuleView } from './CmsModuleView';

export function MenusNavigationView() {
  return (
    <CmsModuleView
      module="menus"
      title="Menus & Navigation"
      subtitle="Configure header, footer and sidebar navigation menus"
    />
  );
}
