import { CmsModuleView } from './CmsModuleView';

export function BackupRestoreView() {
  return (
    <CmsModuleView
      module="backup"
      title="Backup & Restore"
      subtitle="Create backups and restore website content"
    />
  );
}
